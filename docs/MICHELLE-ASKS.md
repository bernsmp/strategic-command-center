# Running list: what we need from Michelle

*One big list for Michelle, so Max can send it all at once instead of piecemeal.
Started 2026-07-20. Add to this file whenever a Michelle dependency surfaces.
Source for most items: the 2026-07-13 three-way call (`docs/CALL-EXTRACT-2026-07-13.md`).*

## Blocking site work (highest priority)

1. **Go High Level webhook URL** for the entry gate. The site's email capture is
   built and deployed but points at nothing — production captures zero contacts
   until we can set `GATE_WEBHOOK_URL`. Ideally: a GHL inbound webhook (or form
   endpoint) that creates a contact from `{email, source, chapter}`. This was
   decided on the Jul 13 call (GHL is the CRM); this URL is the one missing piece.
2. **UTM → JI launch mechanics detail.** She said "probably by next week" on the
   Jul 13 call — that week has passed. We need the exact UTM/code format her
   23-email flow stamps on the PDF so the site's prompt exports can match it.

## Blocking curriculum work (Track 2)

3. **Per-chapter end-of-chapter objectives** (her stated precondition). Path 2
   guided worksheets and the final printed QR codes are both gated on these
   being written down, chapter by chapter.
4. **Path 3 routing-quiz questions** — she required hand-crafted assessment
   questions (not AI boilerplate) routing readers to beginner / advanced /
   quick-start. Even a rough first pass unblocks the build.

## Decisions she co-owns

5. **Master coach name.** "The Resident Strategist" is still a working name;
   final call is Michelle + Mike's.
6. **The Last Door lead offer.** The call narrowed it to a three-path menu
   (How-To / direct-to-Jay via Rob / Mastermind) but the single lead offer is
   still open — needs the three of you.
7. **Proof-of-purchase strictness at the gate.** She leaned soft on the call
   (email capture stays, hard proof optional) — confirm so we can finalize the
   gate copy and flow.

## Smaller / scheduling

8. **Delphi 30-day clock start date** — she was going to confirm (Wed/Thu of the
   week before Jul 13, on the Paul/Tiffany call recording).
9. **Jay's ~2-min per-chapter audio intros** — she said "oh, he'll do it";
   needs her (or Mike) to catch Jay on a phone while walking him through the
   site. Ask: when is that walkthrough happening?
10. **Did Mike demo V1 to Jay last week?** Determines whether the Jay audio
    capture window is already open.

## What Max owes HER (so the list is fair both ways)

- The Roger Rabbit user-journey doc + the Compass HQ curriculum (spec sources
  for attractions/showrooms).
- Report on what Michael Simmons' voice note said.

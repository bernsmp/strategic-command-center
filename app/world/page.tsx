import StepInside from "@/components/StepInside";

// /world is retained as the shareable link + nav target. Same doorway as the
// homepage; both render the shared StepInside component.
export default function WorldPage() {
  return <StepInside />;
}
